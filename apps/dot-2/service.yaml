apiVersion: v1
kind: Service
metadata:
  name: dot-2
spec:
  selector:
    app: dot-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dot-2-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dot-2.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dot-2
#             port:
#               number: 80
