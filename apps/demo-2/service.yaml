apiVersion: v1
kind: Service
metadata:
  name: demo-2
spec:
  selector:
    app: demo-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: demo-2-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: demo-2.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: demo-2
#             port:
#               number: 80
