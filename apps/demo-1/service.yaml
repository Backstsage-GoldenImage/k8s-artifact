apiVersion: v1
kind: Service
metadata:
  name: demo-1
spec:
  selector:
    app: demo-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: demo-1-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: demo-1.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: demo-1
#             port:
#               number: 80
